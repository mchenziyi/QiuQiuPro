# QiuQiuPro TODO 2025-06-23

> 基于近期开发和用户反馈整理的优先事项。

---

## 🔴 Bug

- [ ] **Thread 消息顺序错乱**
      助理回复的文本出现在工具调用之前。
      根因：`streamChat` 先发 `assistant_delta`，`tool_call` / `tool_result` 在 `streamChat` 返回后才发。
      方案：前端或后端调整事件发射顺序，确保工具调用在助理文本之前渲染。

- [ ] **thinking + 工具折叠组未生效**
      `turn-group` 容器未正确渲染。
      上一轮的 `ensureTurnGroup` / `appendThinking` / `addToolRow` 改动未按预期工作。
      方案：debug 前端分组逻辑，验证 DOM 结构和 CSS 类名。

- [ ] **Markdown 渲染未生效**
      `appendAssistant` 和 `addMsg('assistant')` 中的 `renderMarkdown` 调用未显示效果。
      方案：验证 `renderMarkdown` 是否被调用、CSS 样式是否正确覆盖。

- [ ] **Plan 模式 Web 审批流程不完整**
      - 生成方案后确认框缺少详细步骤展示
      - `GeneratePlan` / `ReviewPlan` 为同步 LLM 调用，耗时较长无进度反馈
      - 用户易误以为卡死而切换模式
      方案：Plan 生成期间显示「正在生成方案…」动画；确认框展示完整步骤列表。

- [ ] **mode 切换偶发失效**
      快速多次点击 Status Bar 的 mode 标签可能导致状态不同步。
      方案：`toggleMode` 加防抖或锁定，防止并发请求。

---

## 🟡 功能缺口

- [ ] **Status Bar 展示 plan_mode**
      `state` 事件已含 `plan_mode` 字段，但前端未展示。
      方案：在 Status Bar mode 标签旁加「🔒 只读」标记。

- [ ] **`/api/tools` 端点**
      Agent 端 `ListTools()` 已实现，但 HTTP 端点和前端展示未连通。
      方案：注册路由 + 前端展示可用工具列表。

- [ ] **工具执行超时机制**
      `executeToolCall` 没有 context timeout，MCP 工具卡住会阻塞整个 Agent。
      方案：`context.WithTimeout` 包裹工具执行，默认 60s。

- [ ] **风暴检测可靠性**
      `isErrorResult` 靠关键词匹配错误，易漏判或误判。
      方案：工具返回结构化结果 `{ok, error, data}`。

- [ ] **`all_tools.go` 拆分**
      15 个工具实现在一个文件（~22000 字节），建议按职责拆分。
      方案：拆为 file/edit/search/shell/git/web/todo 等独立文件。

- [ ] **`main.go` 命令注册抽离**
      大量命令 Handler 在 main.go（~20000 字节）。
      方案：命令 Handler 定义抽到 `commands.go`。

---

## 🔵 前端体验

- [ ] **执行过程组默认折叠状态**
      `turn-group` 加载历史时默认折叠，新执行时默认展开。

- [ ] **代码语法高亮**
      Markdown 渲染的 `<code>` 块加语言标识和浅色语法高亮。

- [ ] **Thread 消息视觉分组**
      用户消息 + 执行过程 + 助理回复 用缩进或分隔线分组。

- [ ] **Composer 高度自适应**
      多行输入时输入框自动增长，不出现滚动条。

- [ ] **主题跟随系统**
      `data-theme="auto"` 使用 `prefers-color-scheme`。

---

## 🟢 内部质量

- [ ] **`session.messages` RWMutex**
      防多 goroutine 并发读写 messages。

- [ ] **`web_fetch` HTTP 连接复用**
      包级别复用 `http.Client`。

- [ ] **`event/store.go` 错误处理**
      `Append()` / `Load()` 中忽略的 error。

- [ ] **`SaveCheckpoint` 错误处理**
      `session.Snapshot()` 的错误被忽略。

- [ ] **文档旧工具名同步**
      README / STRUCTURES / docs 中仍有旧工具名。

---

## 标记说明

- `[ ]` 待办
- `[x]` 已完成
